## 2026-08-11

**Status:** done.

**Landed**
- `docs/releasing.md` is the followable checklist: clean checkout → `package:mac` → DMG/zip paths → packaged `doctor` smoke → CLI `npm publish` → GitHub Release upload
- Version alignment table covers `app/package.json` / npm / git tag / Release title / in-app About
- Proven on the `v0.1.1` ship (unsigned prerelease); signing/notarization left to product-trust, not duplicated here
