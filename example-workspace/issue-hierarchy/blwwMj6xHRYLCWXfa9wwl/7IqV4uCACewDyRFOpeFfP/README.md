Without the desktop app, a cloned workspace still needs a public install path for the allocator CLI.

## Resolution (2026-08-11)

Published bare npm package **`pm-all-in-one@0.1.0`**. Cold path: `npx pm-all-in-one …`. App Install Command Line Tool… installs the same command on PATH.

Bare `local-pm` is blocked by npm similarity to `local-pkg`. Standing technical doc: @wiki-MEJy28mhBISjYTGCr8IIo

Child subtasks cover name decision, publish, agent.md, cold-path smoke, and releasing checklist — all `done`.
