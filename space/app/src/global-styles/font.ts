/**
 * `--font-family-*` names from `font.scss` `:root`.
 * Keep in sync with `font.scss` when renaming tokens or stacks.
 */
export const fontFamily = {
  sans: "--font-family-sans",
  serif: "--font-family-serif",
  mono: "--font-family-mono",
  tex: "--font-family-tex",
} as const;
